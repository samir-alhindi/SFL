
enum Weekday = Sunday | Friday | Monday

today = Friday;

show_day day = match day with
    case Sunday -> "Sunday"
    case Friday -> "Friday"
    case Monday -> "Monday"
    else        -> "Please enter a valid Weekday";